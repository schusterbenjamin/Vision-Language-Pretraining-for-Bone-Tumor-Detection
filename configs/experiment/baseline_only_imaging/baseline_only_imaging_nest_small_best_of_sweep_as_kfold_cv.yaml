# @package _global_

defaults:
  - override /data: downstream
  - override /model: only_imaging
  - override /callbacks: default
  - override /trainer: default
  - override /optimizer: adam
  - override /scheduler: no_scheduler

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["nest_small", "only_imaging", "baseline", "k-fold cross validation", "using best config from sweep"]

k_fold_cross_validation: True

trainer:
  max_epochs: 300

data:
  batch_size: 16

model:
  model: nest_small

optimizer:
  lr: 0.00005480785862708334

logger:
  wandb:
    tags: ${tags}
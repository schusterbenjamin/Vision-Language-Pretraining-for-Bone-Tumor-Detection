# @package _global_

defaults:
  - override /data: downstream
  - override /model: fusion
  - override /callbacks: only_btxrd_early_stopping
  - override /trainer: default
  - override /optimizer: adamw
  - override /scheduler: cosine_with_warmup

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["resnet34", "imaging and clinical", "baseline", "try_with_only_n_samples"]

trainer:
  max_epochs: 30

data:
  batch_size: 32
  try_with_only_n_samples: 100

model:
  model: resnet34
  coral_lambda: 0

optimizer:
  lr: 0.0011

logger:
  wandb:
    tags: ${tags}
    project: "setup-testing"